package org.vivus.activemq.authorization;

import org.vivus.activemq.event.EventHandler;

public class LoginEventHandler implements EventHandler<LoginEvent> {
	@Override
	public void handle(LoginEvent event) {
		// TODO
	}
}
